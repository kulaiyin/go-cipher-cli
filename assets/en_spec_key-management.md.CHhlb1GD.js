import{_ as t,o as a,c as d,a0 as o}from"./chunks/framework.t4Rf69I2.js";const g=JSON.parse('{"title":"Key Management Module Specification","description":"","frontmatter":{},"headers":[],"relativePath":"en/spec/key-management.md","filePath":"en/spec/key-management.md","lastUpdated":1785329588000}'),n={name:"en/spec/key-management.md"};function s(i,e,r,c,l,p){return a(),d("div",null,[...e[0]||(e[0]=[o(`<h1 id="key-management-module-specification" tabindex="-1">Key Management Module Specification <a class="header-anchor" href="#key-management-module-specification" aria-label="Permalink to &quot;Key Management Module Specification&quot;">​</a></h1><p>This document describes the design goals of the <code>go-cipher-cli</code> key management module: to implement the key management logic of the peer frontend project <code>frontend-cdn-tools</code> (<code>libs/common-tools</code>) with <strong>byte-level interoperability</strong> — data encrypted by Go can be decrypted by the frontend, and vice versa.</p><h2 id="_1-module-goals" tabindex="-1">1. Module Goals <a class="header-anchor" href="#_1-module-goals" aria-label="Permalink to &quot;1. Module Goals&quot;">​</a></h2><p>Replicate the frontend&#39;s key derivation, password fusion, AES-256-GCM encryption/decryption, and binary container capabilities in Go as a command-line tool, while ensuring interoperability compatibility with the web client.</p><h2 id="_2-peer-frontend-feature-inventory" tabindex="-1">2. Peer Frontend Feature Inventory <a class="header-anchor" href="#_2-peer-frontend-feature-inventory" aria-label="Permalink to &quot;2. Peer Frontend Feature Inventory&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Frontend Function</th><th>Frontend Source File</th><th>Go Implementation</th><th>CLI Command</th></tr></thead><tbody><tr><td><code>KeyDerivation.argon2</code> / <code>hkdf</code></td><td><code>kdf/index.ts</code></td><td><code>internal/kdf.Argon2</code> / <code>HKDF</code></td><td><code>keygen</code></td></tr><tr><td><code>KeyDerivation.generateSalt</code></td><td><code>kdf/index.ts</code></td><td><code>internal/kdf.GenerateSalt</code></td><td><code>keygen</code> (auto-generated)</td></tr><tr><td><code>KeyDerivation.validatePasswordStrength</code></td><td><code>kdf/index.ts</code></td><td><code>internal/kdf.ValidatePasswordStrength</code></td><td>(strength feedback)</td></tr><tr><td><code>KeyDerivation.generateStrongPassword</code></td><td><code>kdf/index.ts</code></td><td><code>internal/kdf.GenerateStrongPassword</code></td><td>(password generation)</td></tr><tr><td><code>normalizePassword</code></td><td><code>password/fusion.ts</code></td><td><code>internal/fusion.NormalizePassword</code></td><td><code>fuse</code> (internal)</td></tr><tr><td><code>safety_merge_strings</code> / <code>fusePasswords</code></td><td><code>password/fusion.ts</code></td><td><code>internal/fusion.fuseMergeStrings</code> / <code>FusePasswords</code></td><td><code>fuse</code></td></tr><tr><td><code>computeFinalPassword</code></td><td><code>password/fusion.ts</code></td><td><code>internal/fusion.ComputeFinalPassword</code></td><td><code>fuse</code></td></tr><tr><td><code>deriveNewSalt</code></td><td><code>password/fusion.ts</code></td><td><code>internal/fusion.DeriveNewSalt</code></td><td>(salt derivation)</td></tr><tr><td><code>AesGcmTools.generate_aes_gcm_key</code></td><td><code>crypto/aes-gcm.ts</code></td><td><code>internal/aesgcm.GenerateAesGcmKey</code></td><td><code>encrypt</code>/<code>decrypt</code> (internal)</td></tr><tr><td><code>AesGcmTools.encryptWithPassword</code> / <code>decryptWithPassword</code></td><td><code>crypto/aes-gcm.ts</code></td><td><code>internal/aesgcm.EncryptWithPassword</code> / <code>DecryptWithPassword</code></td><td><code>encrypt</code> / <code>decrypt</code></td></tr><tr><td><code>CryptoTools.hashText</code></td><td><code>crypto/index.ts</code></td><td><code>internal/crypto.HashText</code></td><td><code>hash</code></td></tr><tr><td><code>HmacTools.hashText</code></td><td><code>crypto/index.ts</code></td><td><code>internal/crypto.HMAC</code></td><td><code>hmac</code></td></tr><tr><td><code>assembleDownloadData</code> / <code>extractDecryptedData</code></td><td><code>data-encryption.ts</code></td><td><code>internal/container.AssembleDownloadData</code> / <code>ExtractDecryptedData</code></td><td><code>encrypt</code>/<code>decrypt</code> (container)</td></tr><tr><td><code>validateHintAndKeysUuidMatch</code></td><td><code>data-encryption.ts</code></td><td><code>internal/container.ValidateHintAndKeysUuidMatch</code></td><td><code>hint-match</code></td></tr><tr><td><code>validateKeyRecovery</code></td><td><code>key-recovery.ts</code></td><td><code>internal/kdf.ValidateKeyRecovery</code></td><td><code>recover</code></td></tr><tr><td><code>KeyDerivationForm.deriveKey</code> (key-set pipeline #1)</td><td><code>KeyDerivationForm.vue</code></td><td><code>internal/kdf.DeriveKeySet</code></td><td><code>key-derive</code></td></tr></tbody></table><h2 id="_3-encryption-pipeline" tabindex="-1">3. Encryption Pipeline <a class="header-anchor" href="#_3-encryption-pipeline" aria-label="Permalink to &quot;3. Encryption Pipeline&quot;">​</a></h2><p>The encryption stack is a chain: <strong>argon2id → HMAC-SHA3-512 → HKDF(SHA3-512) → AES-256-GCM</strong>.</p><h3 id="key-derivation-flow-generate-aes-gcm-key" tabindex="-1">Key Derivation Flow (<code>generate_aes_gcm_key</code>) <a class="header-anchor" href="#key-derivation-flow-generate-aes-gcm-key" aria-label="Permalink to &quot;Key Derivation Flow (\`generate_aes_gcm_key\`)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Input: salt (128-hex string), passwords (string array)</span></span>
<span class="line"><span>1. Filter out empty passwords</span></span>
<span class="line"><span>2. salt_text  = SHA256(pw) computed for each → sorted → joined with colons</span></span>
<span class="line"><span>3. salt_prk   = HMAC-SHA3-512(key=salt, msg=salt_text)        returns a hex string</span></span>
<span class="line"><span>4. Derive 4 subkeys from salt_prk via HKDF (64 bytes each):</span></span>
<span class="line"><span>     s1    = HKDF(salt_prk, info=&quot;argon2id-salt&quot;)</span></span>
<span class="line"><span>     s2    = HKDF(salt_prk, info=&quot;hkdf-safety-key&quot;)</span></span>
<span class="line"><span>     s3    = HKDF(salt_prk, info=&quot;aes-256-gcm-dek&quot;)</span></span>
<span class="line"><span>     sdata = HKDF(salt_prk, info=&quot;aes-256-gcm-data&quot;)</span></span>
<span class="line"><span>5. Harden each weak password (strength &lt; 8 or not 128-hex):</span></span>
<span class="line"><span>     argon2id(pw, salt=s1, t=3, m=32768KiB, p=2, dkLen=64) → hex</span></span>
<span class="line"><span>     then base64-decode(hex) → hex          [frontend quirk, see section 5]</span></span>
<span class="line"><span>6. usr_strong = hardened passwords sorted → joined with colons</span></span>
<span class="line"><span>7. prk_dek    = HMAC-SHA3-512(key=s3, msg=usr_strong)        returns a hex string</span></span>
<span class="line"><span>8. aes_dek    = HKDF(prk_dek, info=&quot;aes-256-gcm-final-key&quot;, L=32)</span></span>
<span class="line"><span>Return: { aes_dek (32 bytes), sdata (64 bytes) }</span></span></code></pre></div><h3 id="aes-256-gcm-encryption-gcmencryptdata" tabindex="-1">AES-256-GCM Encryption (<code>gcmEncryptData</code>) <a class="header-anchor" href="#aes-256-gcm-encryption-gcmencryptdata" aria-label="Permalink to &quot;AES-256-GCM Encryption (\`gcmEncryptData\`)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. iv       = 12 random bytes (stored in plaintext in the output)</span></span>
<span class="line"><span>2. data_prk = HMAC-SHA3-512(key=sdata, msg=iv)               returns a hex string</span></span>
<span class="line"><span>3. iv_used  = HKDF(data_prk, info=&quot;aes-gcm-iv&quot;, L=12)        nonce actually fed to GCM</span></span>
<span class="line"><span>4. AES-256-GCM.Seal(nonce=iv_used, plaintext, tagLen=128)</span></span>
<span class="line"><span>5. Output = iv(12) ‖ ciphertext ‖ tag(16)</span></span></code></pre></div><p>Note: the random <code>iv</code> is what gets stored in the file, but the nonce fed to AES-GCM is <code>iv_used</code>, derived by passing <code>iv</code> through another HMAC+HKDF.</p><h2 id="_4-binary-container-format-little-endian" tabindex="-1">4. Binary Container Format (Little-Endian) <a class="header-anchor" href="#_4-binary-container-format-little-endian" aria-label="Permalink to &quot;4. Binary Container Format (Little-Endian)&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Offset  Length  Field</span></span>
<span class="line"><span>0       4       version      (uint32 LE, = 10000 on encryption)</span></span>
<span class="line"><span>4       4       reserved      (uint32 LE, = 0)</span></span>
<span class="line"><span>8       64      salt_seed     (64 bytes, rendered as 128-hex)</span></span>
<span class="line"><span>72      4       length        (uint32 LE, ciphertext length)</span></span>
<span class="line"><span>76      N       ciphertext    (iv ‖ ciphertext ‖ tag)</span></span></code></pre></div><h2 id="_5-critical-pitfalls-for-byte-compatibility-must-align" tabindex="-1">5. Critical Pitfalls for Byte Compatibility (Must Align) <a class="header-anchor" href="#_5-critical-pitfalls-for-byte-compatibility-must-align" aria-label="Permalink to &quot;5. Critical Pitfalls for Byte Compatibility (Must Align)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>#</th><th>Pitfall</th><th>Description</th></tr></thead><tbody><tr><td>1</td><td>HKDF-Expand is actually full HKDF</td><td>The frontend&#39;s <code>hkdf_expand</code> calls noble&#39;s full <code>hkdf(sha3_512, ikm, salt=empty, info, L)</code>; Go must use <code>hkdf.New(sha3, ikm, nil, info)</code> — a nil salt triggers Extract+Expand</td></tr><tr><td>2</td><td>PRK uses the ASCII bytes of the hex string as IKM/Key</td><td>IKM/Key always uses <code>[]byte(hexString)</code>, never <code>hex.DecodeString</code></td></tr><tr><td>3</td><td>argon2 units are KiB</td><td><code>memorySize</code> is in KiB; hardening params <code>t=3, m=32768, p=2, dkLen=64, salt=s1</code>; the frontend noble&#39;s <code>m/=1024</code> only takes effect when <code>NODE_ENV===&quot;test&quot;</code> — Go does not replicate this</td></tr><tr><td>4</td><td>The base64 quirk in <code>deriveStrongPassword</code></td><td>The frontend <strong>misinterprets argon2&#39;s hex output as base64</strong> (<code>base64ToBytes(hashHex)</code>) and re-encodes to hex into <code>processedPasswords</code>. This is the actual frontend behavior (likely a bug) — Go must replicate it</td></tr><tr><td>5</td><td><code>deriveNewSalt</code>&#39;s salt uses ASCII bytes</td><td>In <code>KeyDerivation.argon2(originalSalt, {salt: originalSalt})</code>, the salt is a string passed as ASCII bytes — do not hex-decode it</td></tr><tr><td>6</td><td>The <code>[object Object]</code> coercion in the key-set pipeline</td><td><code>KeyDerivationForm.vue:697-699</code> assigns <code>input_sha3 = CryptoTools.hashText(...)</code> (a Result <strong>object</strong>) and then builds <code>combinedPassword = passwordInput + input_sha3</code>. JS string-coerces the object to <code>&quot;[object Object]&quot;</code>, so the SHA3-512 hex is <strong>never</strong> used in <code>combinedPassword</code>. Go (<code>internal/kdf.DeriveKeySet</code>) must replicate this by using the literal <code>password + &quot;[object Object]&quot;</code> — the hash is still computed for parity but its value is discarded</td></tr><tr><td>7</td><td>The <code>array_buffer_to_string</code> UTF-8 quirk in the key-set pipeline</td><td><code>KeyDerivationForm.vue:703</code> passes the 64-byte <code>salt_password</code> HKDF output through <code>AesGcmTools.array_buffer_to_string</code>, i.e. a WHATWG <code>TextDecoder</code> (error mode = replacement). Illegal byte sequences become U+FFFD, so the re-encoded HMAC key (127 bytes) differs from the raw 64 bytes. Go (<code>internal/kdf.utf8DecodeBytes</code>) implements the WHATWG UTF-8 decoder state machine to stay byte-compatible; the standard library has no equivalent of the &quot;replacement&quot; error mode</td></tr></tbody></table><h2 id="_6-golden-vector-verification-mechanism" tabindex="-1">6. Golden Vector Verification Mechanism <a class="header-anchor" href="#_6-golden-vector-verification-mechanism" aria-label="Permalink to &quot;6. Golden Vector Verification Mechanism&quot;">​</a></h2><p>To ensure byte-level interoperability, a <strong>three-party verification</strong> approach is adopted:</p><ol><li><strong>Golden vector generation</strong>: the frontend project&#39;s <code>libs/common-tools/scripts/gen-vectors.mjs</code> runs with <code>NODE_ENV=production</code> to produce deterministic output, frozen into <code>internal/testvectors/vectors.json</code>.</li><li><strong>Real source authoritative test</strong>: the frontend&#39;s <code>libs/common-tools/tests/authoritative-*.test.ts</code> directly invokes the real <code>AesGcmTools</code> / <code>deriveNewSalt</code> / <code>validateHintAndKeysUuidMatch</code> / <code>validateKeyRecovery</code>, asserting consistency with <code>vectors.json</code>.</li><li><strong>Go test</strong>: each <code>internal/*</code> package&#39;s test loads <code>vectors.json</code> for golden vector comparison.</li></ol><p>Three-party consistency proves: real source ↔ vectors.json ↔ Go are byte-aligned across the entire chain.</p><h3 id="regenerating-golden-vectors" tabindex="-1">Regenerating Golden Vectors <a class="header-anchor" href="#regenerating-golden-vectors" aria-label="Permalink to &quot;Regenerating Golden Vectors&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /works/gitworks/frontend-cdn-tools/libs/common-tools</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">NODE_ENV</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">production</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> scripts/gen-vectors.mjs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /works/gitworks/go-cipher-cli/internal/testvectors/vectors.json</span></span></code></pre></div><h3 id="running-the-frontend-authoritative-tests" tabindex="-1">Running the Frontend Authoritative Tests <a class="header-anchor" href="#running-the-frontend-authoritative-tests" aria-label="Permalink to &quot;Running the Frontend Authoritative Tests&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /works/gitworks/frontend-cdn-tools/libs/common-tools</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">NODE_ENV</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">production</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vitest</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  tests/authoritative-vector.test.ts</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  tests/authoritative-derivables.test.ts</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  tests/authoritative-web-utils.test.ts</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --testTimeout=60000</span></span></code></pre></div><h2 id="_7-go-library-selection" tabindex="-1">7. Go Library Selection <a class="header-anchor" href="#_7-go-library-selection" aria-label="Permalink to &quot;7. Go Library Selection&quot;">​</a></h2><p>Everything uses the standard library + <code>golang.org/x/crypto</code> + <code>golang.org/x/text</code>; no third-party crypto libraries are introduced:</p><table tabindex="0"><thead><tr><th>Purpose</th><th>Dependency</th></tr></thead><tbody><tr><td>Argon2id</td><td><code>golang.org/x/crypto/argon2</code></td></tr><tr><td>SHA3-512 / SHA3 family</td><td><code>golang.org/x/crypto/sha3</code></td></tr><tr><td>HKDF(SHA3-512)</td><td><code>golang.org/x/crypto/hkdf</code></td></tr><tr><td>HMAC(SHA3-512/SHA2)</td><td>Standard library <code>crypto/hmac</code></td></tr><tr><td>SHA-256 / MD5 / SHA1 / SHA2</td><td>Standard library</td></tr><tr><td>AES-256-GCM</td><td>Standard library <code>crypto/aes</code> + <code>crypto/cipher</code></td></tr><tr><td>Secure random numbers</td><td>Standard library <code>crypto/rand</code></td></tr><tr><td>Unicode NFC</td><td><code>golang.org/x/text/unicode/norm</code></td></tr><tr><td>Base64 / Hex</td><td>Standard library <code>encoding/*</code></td></tr><tr><td>Little-endian binary container</td><td>Standard library <code>encoding/binary</code></td></tr><tr><td>CLI / config / logging / progress</td><td>cobra / viper / zap / survey / mpb</td></tr></tbody></table><h2 id="_8-cli-commands" tabindex="-1">8. CLI Commands <a class="header-anchor" href="#_8-cli-commands" aria-label="Permalink to &quot;8. CLI Commands&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Command</th><th>Purpose</th><th>Corresponding Frontend Capability</th></tr></thead><tbody><tr><td><code>encrypt [file] -p &lt;pw&gt; [--salt &lt;hex&gt;]</code></td><td>AES-256-GCM encrypt a file, output a frontend-compatible container</td><td><code>encryptWithPassword</code> + <code>assembleDownloadData</code></td></tr><tr><td><code>decrypt [file] -p &lt;pw&gt;</code></td><td>Decrypt a container (salt is read from the container)</td><td><code>decryptWithPassword</code> + <code>extractDecryptedData</code></td></tr><tr><td><code>keygen -p &lt;pw&gt; [--salt &lt;hex&gt;] [--hash-length N]</code></td><td>argon2id key derivation (multiple passwords go through fusion)</td><td><code>KeyDerivation.argon2</code> + <code>computeFinalPassword</code></td></tr><tr><td><code>hash [text] --algo &lt;name&gt;</code></td><td>Hash text (MD5/SHA1/SHA2/SHA3)</td><td><code>CryptoTools.hashText</code></td></tr><tr><td><code>hmac [text] --algo &lt;name&gt; --key &lt;k&gt;</code></td><td>Compute HMAC</td><td><code>HmacTools.hashText</code></td></tr><tr><td><code>fuse --salt &lt;s&gt; -p &lt;pw&gt;...</code></td><td>Password fusion</td><td><code>computeFinalPassword</code></td></tr><tr><td><code>recover [key] --uuid &lt;u&gt;...</code></td><td>Key recovery validation</td><td><code>validateKeyRecovery</code></td></tr><tr><td><code>hint-match --encrypted &lt;t&gt; --meta &lt;t&gt;</code></td><td>Hint/UUID match validation</td><td><code>validateHintAndKeysUuidMatch</code></td></tr></tbody></table><h2 id="_9-package-structure" tabindex="-1">9. Package Structure <a class="header-anchor" href="#_9-package-structure" aria-label="Permalink to &quot;9. Package Structure&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>internal/</span></span>
<span class="line"><span>├── safety/        # HKDF / HMAC / argon2 / random / encoding (low-level primitives)</span></span>
<span class="line"><span>├── kdf/           # KeyDerivation API (argon2 / hkdf / salt / strength / recovery)</span></span>
<span class="line"><span>├── fusion/        # Password fusion (normalize / merge / fuse / deriveNewSalt)</span></span>
<span class="line"><span>├── aesgcm/        # AES-256-GCM key derivation + encryption/decryption</span></span>
<span class="line"><span>├── container/     # Binary container + UUID matching</span></span>
<span class="line"><span>├── crypto/        # hash / HMAC / Base64 facade</span></span>
<span class="line"><span>└── testvectors/   # Golden vectors (embedded vectors.json + loader)</span></span>
<span class="line"><span>cmd/</span></span>
<span class="line"><span>├── encrypt.go / decrypt.go / keygen.go / utils.go</span></span>
<span class="line"><span>└── cli_e2e_test.go / utils_e2e_test.go   # Isolated-process end-to-end tests</span></span></code></pre></div><h2 id="_10-test-matrix" tabindex="-1">10. Test Matrix <a class="header-anchor" href="#_10-test-matrix" aria-label="Permalink to &quot;10. Test Matrix&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Module</th><th>Test Content</th><th>Vector Source</th></tr></thead><tbody><tr><td><code>safety</code></td><td>HKDF-SHA3-512 / HMAC-SHA3-512 / argon2id / SHA256 / encoding / random / high-strength detection</td><td>gen-vectors</td></tr><tr><td><code>kdf</code></td><td>argon2 hardening chain / validatePasswordStrength / generateSalt / generateStrongPassword / validateKeyRecovery</td><td>gen-vectors + frontend kdf.test.ts</td></tr><tr><td><code>fusion</code></td><td>normalizePassword / safetyMergeStrings / fusePasswords (incl. Chinese) / deriveNewSalt</td><td>gen-vectors + frontend password-fusion.test.ts</td></tr><tr><td><code>aesgcm</code></td><td>generate_aes_gcm_key full pipeline / fixed-IV encryption / round-trip / wrong password failure / invalid input</td><td>gen-vectors (authoritative)</td></tr><tr><td><code>container</code></td><td>assemble/parse round-trip / short data / wrong length / salt extraction / UUID matching</td><td>gen-vectors + inline</td></tr><tr><td><code>crypto</code></td><td>MD5/SHA1/SHA2/SHA3 known vectors / HMAC / Base64 round-trip</td><td>frontend crypto.test.ts</td></tr><tr><td><code>cmd</code></td><td>End-to-end for each subcommand (isolated process) + encrypt/decrypt round-trip + wrong password</td><td>inline + golden vectors</td></tr></tbody></table><h2 id="_11-known-uncovered-items" tabindex="-1">11. Known Uncovered Items <a class="header-anchor" href="#_11-known-uncovered-items" aria-label="Permalink to &quot;11. Known Uncovered Items&quot;">​</a></h2><ul><li><strong>RSA encryption/decryption</strong> (frontend <code>jsencrypt</code>): weakly related to the key management mainline; jsencrypt&#39;s padding mode needs separate verification, not yet implemented.</li><li><strong>Key derivation config file export</strong> (frontend <code>KeyDerivationForm.downloadKeys</code>): generates a <code>.txt</code> config file containing <code>uuid</code>/<code>salt</code>/<code>hint_ids</code>/<code>uuids</code> for key recovery when the password is forgotten. Currently Go only implements the <strong>verification side</strong> (<code>recover</code>/<code>hint-match</code>), not the <strong>generation side</strong> (<code>maskKey</code> masking + config export).</li></ul>`,36)])])}const y=t(n,[["render",s]]);export{g as __pageData,y as default};
